---
import NinaMark from "./NinaMark.astro";

interface Props {
  compact?: boolean;
}

const { compact = false } = Astro.props;
---

<a class:list={["brand", { compact }]} href="/" aria-label="Nina, início">
  <NinaMark size={compact ? 26 : 30} class="brand-mark" />
  <span>Nina</span>
</a>
